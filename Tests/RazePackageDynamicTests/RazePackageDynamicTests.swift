import XCTest
@testable import RazePackageDynamic

final class RazePackageDynamicTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RazePackageDynamic().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
